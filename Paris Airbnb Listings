aaaaaaaaa

````python
import pandas as pd
import numpy as np
listings = pd.read_csv('Listings.csv', encoding='latin-1')

## change the data type of 'host_since' column to datetime
listings['host_since'] = pd.to_datetime(listings['host_since'], errors='coerce')

print(listings['host_since'].dtype)

listings.to_csv('Listings_cleaned.csv', index=False)
````
